/**
 * Component - View Settins displaying the Settings page
 * @module Settings
 * @returns { HTMLElement } HTMLElement
 */
const Settings = () => {
  return (
    <div className="Settings">
      Page "Réglage"
    </div>
  );
};

export default Settings;